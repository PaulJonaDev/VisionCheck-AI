Frontend (UI móvil y web) para VisionCheck AI

- Código actual del frontend: `../visioncheck-frontend` (Expo React Native)
- Cómo ejecutar:
  - `cd ../visioncheck-frontend && npm install && npm run web`
- Documentación: `../docs/MASTER.md` y `../docs/OVERVIEW.md`

Nota: Este directorio actúa como índice del frontend hasta migrar el código a `frontend/`.
